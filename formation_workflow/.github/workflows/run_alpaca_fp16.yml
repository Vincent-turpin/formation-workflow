# THIS FILE WAS GENERATED BY HIGGSFIELD. 
# DO NOT EDIT. 
# IF YOUR WORKFLOW DOESN'T WORK, CREATE AN ISSUE.

name: Run alpaca_fp16

on:

  workflow_dispatch:
    inputs:
      run_name: 
        description: 'Name of the run, if not set will be chosen randomly, if exists will be reused'
        required: false
      
      seed:
        required: True
        default: 42
      size:
        required: False
        default: 7b
        options: ['7b', '13b', '70b']
        type: choice
      num_epochs:
        description: Number of epochs
        required: False
        default: 1


    
concurrency:
  cancel-in-progress: false
  group: main


jobs:
  run-training:
    name: Run experiment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Install invoker
      run: |
        wget https://github.com/ml-doom/invoker/releases/download/latest/invoker-latest-linux-amd64.tar.gz
        tar -xvf invoker-latest-linux-amd64.tar.gz
        sudo mv invoker /usr/bin/invoker
        rm invoker-latest-linux-amd64.tar.gz
    - name: Set Port and Run Name
      run: |
          pip install higgsfield==0.0.3 --quiet
          
          echo "SSH_KEY='NOTHING'" >> env
          eval "$(jq -r '["key", "user", "port", "hosts"] as $names | . as $json | $names[] as $name | "export \($name | ascii_upcase | @sh)=\($json[$name] | @sh)"' < <(higgsfield ci get-ssh-details))" 
          
          echo "HOSTS=$HOSTS" >> $GITHUB_ENV
          echo "USER=$USER" >> $GITHUB_ENV
          echo "PORT=$PORT" >> $GITHUB_ENV

          echo "CHOSEN_PORT=$(invoker random-port)" >> $GITHUB_ENV
          echo "CHOSEN_RUN_NAME=$(invoker random-name)" >> $GITHUB_ENV
          echo "NPROC_PER_NODE=$(higgsfield ci get-nproc-per-node)" >> $GITHUB_ENV
    - name: Run experiment
      uses: appleboy/ssh-action@v1.0.0
      env: 
        RUN_PORT: ${{ env.CHOSEN_PORT }}
        RUN_NAME: ${{ github.event.inputs.run_name || env.CHOSEN_RUN_NAME }}
        NPROC_PER_NODE: ${{ env.NPROC_PER_NODE }}
        HOSTS: ${{ env.HOSTS }}
        USER: ${{ env.USER }}
        PORT: ${{ env.PORT }}
      with:
        host: ${{ env.HOSTS }}
        username: ${{ env.USER }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ env.PORT }}
        script: |
          cd ~/higgsfield/formation_workflow
          echo "SSH_KEY=NOTHING" > env
          echo WAN_DB_TOKEN="${{ secrets.WAN_DB_TOKEN }}" >> env
          echo HUGGINGFACE_TOKEN="${{ secrets.HUGGINGFACE_TOKEN }}" >> env
          invoker experiment run --project_name formation_workflow --experiment_name alpaca_fp16  --run_name ${{ env.RUN_NAME }} --port ${{ env.RUN_PORT }} --nproc_per_node ${{ env.NPROC_PER_NODE }} --hosts ${{ env.HOSTS }}  hf_action_seed="${{ github.event.inputs.seed }}" hf_action_size="${{ github.event.inputs.size }}" hf_action_num_epochs="${{ github.event.inputs.num_epochs }}"


